[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --cov=core.schemas
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-branch
