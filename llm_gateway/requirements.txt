# requirements.txt
# Core dependencies
pydantic>=2.0.0
httpx>=0.24.0

# Optional dependencies (install as needed)
# openai>=1.0.0        # For OpenAI adapter
# anthropic>=0.25.0    # For Anthropic adapter (if using official SDK)
# google-generativeai  # For Gemini adapter (if using official SDK)
