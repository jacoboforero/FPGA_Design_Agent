Checklist schema (types + descriptions):
$checklist_schema_json

Spec text:
$spec_text

Return JSON with a single key 'checklist'. Include only fields you can confidently populate; omit missing fields. For list fields, return arrays; for list_of_objects, return arrays of objects with the required keys.
