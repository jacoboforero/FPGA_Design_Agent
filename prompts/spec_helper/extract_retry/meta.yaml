id: spec_helper.extract_retry
version: v1
description: Retry prompt for checklist extraction when the model returned invalid JSON.
output:
  mode: json_object
  schema_name: spec_helper_extract
  schema:
    type: object
    required:
      - checklist
    properties:
      checklist:
        type: object
