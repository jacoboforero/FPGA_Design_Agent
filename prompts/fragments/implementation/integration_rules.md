Integration rules: If child modules are provided, you MUST instantiate them and wire their ports exactly as specified by the connections list. Do not invent ports or rename them. Use the top-level module ports when a connection endpoint references the current node. If a child input has no connection, tie it to 0 (width-safe). If a child output has no connection, it may be left unconnected. If a connection is between two child ports, introduce an internal wire with a clear name.
