Generate synthesizable RTL for a SystemVerilog-capable benchmark harness.
Rules: no code fences. SystemVerilog is allowed when it improves clarity (including logic/always_ff/always_comb), but do not use interfaces, classes, packages, or multiple top modules. If no clock is provided, emit pure combinational logic with continuous assigns and/or always_comb only. Implement the behavior exactly as described by the prompt; do not invent features not stated. Prefer lint-clean RTL under Verilator: avoid constant comparisons due to bit-width (e.g., don't compare a 3-bit signal to > 7), avoid unused signals, and avoid self-assignments like `x <= x`.
